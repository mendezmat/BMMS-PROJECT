# Update 037.3 Validation — Smart Flyer Operator Workspace

## Implemented

- Collapsible Source and Inspector panels with persistent local preferences.
- Large-preview workspace states when either or both side panels are hidden.
- Compact template switcher above the real 16:9 renderer.
- Keyboard shortcuts: `1` Modern, `2` TV, `3` Glass, `Enter` TAKE.
- Active template synchronization between quick switcher, proposal cards and renderer.
- Existing Preview/Program API and Smart Flyer overlay contract preserved.
- Scripture implementation unchanged.

## Quality gate

- Repository check: run with `npm run check`.
- Automated tests: run with `npm test`.
- ZIP integrity: validate after packaging.
