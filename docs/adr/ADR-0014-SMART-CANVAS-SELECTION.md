# ADR-0014 — Smart Canvas selects Scene Nodes

El editor simple ya no trata la composición como un rectángulo aislado. La
selección visual representa el nodo `scripture-composition` del documento
`bmms.scene`. Guías, Snap y restricciones se almacenan en el comportamiento del
mismo nodo. El futuro panel Layers reutilizará esta selección.
