# ADR-0013 — Scene Engine as canonical document model

BMMS adopta `bmms.scene` como documento común para Graphics. Durante la
migración, Scripture continúa operando con el estado estable y el servidor lo
refleja automáticamente en una Scene. El futuro modo avanzado expondrá este
mismo árbol, sin convertir ni duplicar diseños.
