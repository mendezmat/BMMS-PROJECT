# Release Policy

BMMS uses Semantic Versioning.

A release requires passing checks, an updated changelog, migration notes when
needed, a version update, a Git tag, a generated release archive and checksum.
