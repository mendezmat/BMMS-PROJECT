import test from "node:test";import assert from "node:assert/strict";import {defaultAppState} from "../../packages/shared/src/default-app-state.js";test("restores original Scripture geometry",()=>{assert.equal(defaultAppState.scripture.composition.width,1660);assert.equal(defaultAppState.scripture.composition.bottom,28);assert.equal(defaultAppState.scripture.composition.horizontalPadding,72)});test("restores writing and contextual transitions",()=>{const a=defaultAppState.scripture.animation;assert.equal(a.wordCascade,true);assert.equal(a.wordCascadeStepMs,18);assert.equal(a.sameChapterOutMs,100);assert.equal(a.sameChapterInMs,170);assert.equal(a.chapterChangeMs,320);assert.equal(a.bookChangeMs,420)});test("restores gradient and typography",()=>{assert.equal(defaultAppState.scripture.gradient.mode,"adaptive");assert.equal(defaultAppState.scripture.appearance.titleFont,"Montserrat");assert.equal(defaultAppState.scripture.appearance.bodySize,72)});